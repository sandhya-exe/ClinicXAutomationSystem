@model IEnumerable<ClinicXAutomationSystem.Models.supplier>

@{
    ViewBag.Title = "Index";
}

<h2>All Suppliers</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contact)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.contact)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                <!--@Html.ActionLink("Edit", "SupplierEdit", new { id = item.supplierID }) |-->
                @*@Html.ActionLink("Details", "SupplierDetails", new { id = item.supplierID }) |*@
                <!--@Html.ActionLink("Delete", "SupplierDelete", new { id = item.supplierID })-->
                @Html.ActionLink("Edit", "SupplierEdit", new { id = item.supplierID }, new { @class = "btn btn-outline-primary me-2" }) 
                @*@Html.ActionLink("Delete", "SupplierDelete", new { id = item.supplierID }, new { @class = "btn btn-outline-danger" })*@

            </td>
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Back", "Index", null, new { @class = "btn btn-outline-secondary btn-sm" })
</div>
<br />
<p>
    @Html.ActionLink("Create New", "SupplierCreate")
</p>

